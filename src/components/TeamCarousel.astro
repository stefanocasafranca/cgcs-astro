---
const teamMembers = [
  { name: 'Bryan Port', role: 'Director, CGCS', image: 'https://i0.wp.com/cgcsacc.org/wp-content/uploads/2022/10/Bryan-ai.png?fit=800%2C750&ssl=1' },
  { name: 'Marisela Perez', role: 'Strategic Planner in Civic Leadership', image: 'https://i0.wp.com/cgcsacc.org/wp-content/uploads/2022/10/Marisela-ai.png?fit=800%2C750&ssl=1' },
  { name: 'Austin Wells', role: 'Strategic Planner for Community Relations and Environmental Affairs', image: 'https://i0.wp.com/cgcsacc.org/wp-content/uploads/2025/08/DSC_0025-e1754946625740.png?fit=667%2C636&ssl=1' },
  { name: 'Cate Thomason', role: 'Strategic Planner in Public Service', image: 'https://i0.wp.com/cgcsacc.org/wp-content/uploads/2022/10/Cate-ai.png?fit=800%2C750&ssl=1' },
  { name: 'Stefano Casafranca', role: 'Strategic Planner in Citizenship & Tech (UX/AI Technologies)', image: 'https://i0.wp.com/cgcsacc.org/wp-content/uploads/2025/08/Screenshot-2025-08-11-at-5.58.15-AM-2-e1754910275266.png?fit=612%2C570&ssl=1' },
];
---

<section class="py-16 bg-gray-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="relative">
      <!-- Carousel Container -->
      <div id="team-carousel" class="overflow-hidden">
        <div id="team-track" class="flex transition-transform duration-500 ease-in-out">
          {teamMembers.map((member, index) => (
            <div class="flex-shrink-0 w-full sm:w-1/2 md:w-1/3 lg:w-1/4 px-4">
              <div class="bg-white rounded-2xl overflow-hidden shadow-lg">
                <div class="aspect-square overflow-hidden">
                  <img
                    src={member.image}
                    alt={member.name}
                    class="w-full h-full object-cover"
                  />
                </div>
                <div class="p-6 text-center h-[140px] flex flex-col justify-center">
                  <h3 class="text-xl font-bold text-text-dark">{member.name}</h3>
                  <p class="text-text-light mt-1 text-sm leading-tight">{member.role}</p>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- Navigation Buttons -->
      <button
        id="prev-btn"
        class="absolute left-0 top-1/2 -translate-y-1/2 -translate-x-4 w-12 h-12 bg-white rounded-full shadow-lg flex items-center justify-center hover:bg-gray-50 transition-colors z-10"
        aria-label="Previous slide"
      >
        <svg class="w-6 h-6 text-text-dark" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
        </svg>
      </button>
      <button
        id="next-btn"
        class="absolute right-0 top-1/2 -translate-y-1/2 translate-x-4 w-12 h-12 bg-white rounded-full shadow-lg flex items-center justify-center hover:bg-gray-50 transition-colors z-10"
        aria-label="Next slide"
      >
        <svg class="w-6 h-6 text-text-dark" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
        </svg>
      </button>
    </div>

    <!-- Dots Navigation (dynamically populated based on visible slides) -->
    <div id="dots-container" class="flex justify-center mt-8 gap-2"></div>
  </div>
</section>

<script>
  const track = document.getElementById('team-track') as HTMLElement;
  const prevBtn = document.getElementById('prev-btn');
  const nextBtn = document.getElementById('next-btn');
  const dotsContainer = document.getElementById('dots-container') as HTMLElement;

  let currentIndex = 0;
  const totalSlides = 5;

  function getVisibleSlides() {
    if (window.innerWidth >= 1024) return 4;
    if (window.innerWidth >= 768) return 3;
    if (window.innerWidth >= 640) return 2;
    return 1;
  }

  function getMaxIndex() {
    return Math.max(0, totalSlides - getVisibleSlides());
  }

  function createDots() {
    const maxIndex = getMaxIndex();
    const numDots = maxIndex + 1;

    dotsContainer.innerHTML = '';

    for (let i = 0; i < numDots; i++) {
      const dot = document.createElement('button');
      dot.className = 'dot w-3 h-3 rounded-full bg-gray-300 transition-colors';
      dot.setAttribute('data-index', i.toString());
      dot.setAttribute('aria-label', `Go to slide ${i + 1}`);
      dot.addEventListener('click', () => {
        currentIndex = i;
        updateCarousel();
      });
      dotsContainer.appendChild(dot);
    }
  }

  function updateCarousel() {
    const visibleSlides = getVisibleSlides();
    const slideWidth = 100 / visibleSlides;
    track.style.transform = `translateX(-${currentIndex * slideWidth}%)`;

    const dots = dotsContainer.querySelectorAll('.dot');
    dots.forEach((dot, index) => {
      dot.classList.toggle('bg-primary', index === currentIndex);
      dot.classList.toggle('bg-gray-300', index !== currentIndex);
    });
  }

  prevBtn?.addEventListener('click', () => {
    const maxIndex = getMaxIndex();
    if (currentIndex > 0) {
      currentIndex--;
    } else {
      // Loop to the end
      currentIndex = maxIndex;
    }
    updateCarousel();
  });

  nextBtn?.addEventListener('click', () => {
    const maxIndex = getMaxIndex();
    if (currentIndex < maxIndex) {
      currentIndex++;
    } else {
      // Loop to the beginning
      currentIndex = 0;
    }
    updateCarousel();
  });

  window.addEventListener('resize', () => {
    const maxIndex = getMaxIndex();
    // Ensure currentIndex is valid after resize
    if (currentIndex > maxIndex) {
      currentIndex = maxIndex;
    }
    createDots();
    updateCarousel();
  });

  // Initialize
  createDots();
  updateCarousel();
</script>
