# Project Rules

## Git Workflow

- After any code change, **automatically push to GitHub**
- Push to a feature branch, not main
- Only merge to main after user approval
- After merging to main, create a new branch for the next work
